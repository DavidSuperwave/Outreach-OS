{
  "name": "cloudflare-os-starter",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.9.0",
  "engines": {
    "node": ">=24 <25",
    "pnpm": ">=11 <12"
  },
  "scripts": {
    "test": "node --test scripts/*.test.mjs && pnpm --filter custom-gatekeeper test && pnpm --filter error-reporter test && pnpm --filter identity test && pnpm --filter registry test && pnpm --filter authz test && pnpm --filter control-plane test && pnpm --filter soup test && pnpm --filter shell test && pnpm --filter task-slice test && pnpm --filter connectivity test",
    "governance": "node scripts/governance/kernel-budget.mjs && node scripts/governance/rpc-contract.mjs",
    "check": "node scripts/deploy.mjs --check",
    "deploy": "node scripts/deploy.mjs"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.20.1",
    "jsonc-parser": "^3.3.1",
    "typescript": "^5.9.3",
    "vitest": "^4.1.10",
    "wrangler": "^4.115.0"
  }
}
