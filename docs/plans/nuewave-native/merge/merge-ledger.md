# Merge ledger — every capability, one verdict

> Created 2026-08-19. The backbone of the merge audit (ruling 7: re-audit
> everything). One row per capability at the granularity a verdict is about.
> `Verdict:` slots are filled **only by David's dated rulings** — everything
> else in a row is research and may be refined by agents. Prior verdicts from
> the concept-only docs are recorded as *provisional* and carry no authority.
>
> Row lifecycle: `☐ unresearched` → `◐ researched` (pointers + proposal
> filled, cross-refs to the inventories) → `✔ ruled` (verdict + date).
> When every row is `✔`, this file plus dates is the final audit the Linear
> scope-map agent builds "Nuewave T" from.

Pinned source: `DavidSuperwave/Neuwave@9f7a26b` — see `README.md`.
Cross-refs: `endpoint-inventory-backend.md` (EB), `endpoint-inventory-frontend.md` (EF), `schema-harvest.md` (SH).

> **Default ruling (2026-08-19, Q19):** every row not explicitly ruled or
> parked is **KEEP — faithful recreation**. Kills happen only by explicit
> ruling (so far: `graphql_soup`, FusionAuth, and the AWS substrate itself).
> **Parked to the end:** the business chrome — billing/paywall, onboarding,
> getting-started — because David plans something different there; its
> *primitives* (how the old flows/pricing/first-run mechanically work) still
> get documented from source as reference for that redesign.

## 1. Backend domains — DSS composition root (`/dss`)

Mounted domain crates per `../reference/platform-context/platform-canvas.md`;
pointers to be verified against `services/document_storage_service` and
`crates/*` during research.

| St | Capability | Old source | What it is | Provisional (old docs) | Proposed CF target | Verdict |
|---|---|---|---|---|---|---|
| ◐ | documents | `crates/documents` (18,653 LOC / 76 files); router `crates/documents/src/inbound/axum_router.rs:217-320`; content model `crates/documents/src/domain/content.rs:12-176`; events `crates/documents/src/domain/events.rs:162-190`; tables `crates/macro_db_client/migrations/0001_baseline.sql:325-470`; file types `crates/model_file_type/src/lib.rs:51,230-249` | **The polymorphic core entity, not one capability.** 25 endpoints on `/documents` from two codebases (22 hex + 3 DSS-native, `services/document_storage_service/src/api/documents/mod.rs:49-62`); **5 creation flavors** (document, task, markdown, snippet, skill — last three behind `#[cfg(feature = "document_create")]`); **15 tables** incl. `DocumentInstance` (versions, sha-keyed), `DocumentFamily`+`branchedFrom*` (branching), `DocumentBom`/`BomPart` (docx), `DocumentText*`; **8 Kafka events** on `macro.documents`; 5 agent tools (frozen ai_toolset applies); 413 `FileType` variants → 18 viewer associations. **Content lives in one of 5 locations** (`object_storage`, `sync_service`, `docx_bom_parts`, `converted_pdf`, `unknown`) — so the domain does **not** own its bytes: live-collab content is held by the lifted `sync-service`. Carries the threads/comments/PDF-annotation stack and the task system (duplicates, branch names, GitHub PRs). Full audit: `audits/documents-audit.md`. | Keep | Entity registry + per-entity DO under **D1/1a** (the `ensure_document_exists` middleware hop is what the registry replaces); content-location indirection preserved with R2 for `object_storage` and the **lifted `sync-service` (A3, mounted per L1 `/sync`)** as an authoritative backend it doesn't control; **materialized-index layer (D1/1a, required)** for lists; 8 events → intent record + in-DO alarm with idempotent consumers under **D3/3a**, except `document.interaction` which is presence and belongs with **C1** kernel session events; 25 endpoints collapse to RPC methods under **R3** (the DSS double-mount at root *and* `/dss`, `api/mod.rs:290-291`, has no successor); per-type sharing stays per-type code per **D1/1a**. Effort: **largest single row in the ledger** — audit §G offers three splits (G1 core/annotations/tasks, G2 one row with sub-scope, G3 split tasks only). | — |
| ◐ | projects (=folders) | `crates/projects` (9,809 LOC / 45 files); router `crates/projects/src/inbound/axum_router.rs:119-175`; table `crates/macro_db_client/migrations/0001_baseline.sql` `"Project"`; events `crates/projects/src/domain/events.rs` | Folder tree over a **self-referential `Project.parentId`** — this is the Project=Folder invariant in schema form. 11 endpoints: CRUD + `/content`, `/permissions`, `/access_level`, `/revert_delete` (soft-delete restore), `/permanent` (hard delete), plus `/pending`, `/preview` (batch), `/upload` and `/upload_extract`. Columns `uploadPending` + `uploadRequestId` make folder upload an **async, resumable job**: the row is written pending, the `upload_extractor_lambda_trigger`→`_handler` pair expands the archive, and progress reaches the client as `UploadFolderStatusUpdate` (`completed`/`partially_completed`/`failed`/`unknown`) over connection_gateway. **6 events** on `macro.projects`: `project.created/updated/deleted/restored/permanently_deleted/uploaded`. Sharing via `ProjectPermission` → `SharePermission`. | Keep | Tree in typed-storage with the parent edge indexed via the **D1/1a materialized-index layer** (folder listing is the hot read); soft-delete + restore + purge is a three-state lifecycle that wants the entity registry's tombstones (**D1/1a**). Folder upload is the clearest **D3/3a + D4/4a** case in the ledger: `uploadPending`/`uploadRequestId` become in-DO intent + alarm, the Lambda trigger/handler pair collapses into one DO (see `audits/lambda-batch-families-audit.md` §B/§E1), and the status updates become **C1** kernel session events instead of gateway pushes. 6 events under **D3/3a**. Effort: medium — the tree is small, the async upload pipeline is the real work. | — |
| ✔ | channels | `crates/channels` | Channels containers | ~~Conflict: keep-thin vs drop~~ | DO-backed channels + messaging | **Keep full — faithful recreation incl. messaging surface (2026-08-19)** |
| ✔ | soup | `crates/soup` | The list/feed engine behind Soup lists | Keep (thin) | Typed RPC + DO queries, WebSocket push | **Faithful UX on native data (2026-08-19)** — list behavior recreated exactly; substrate is CF-OS-native |
| ✔ | graphql_soup | `crates/graphql_soup` | GraphQL surface over soup | — | — | **Killed (2026-08-19)** — internal wire detail; replaced by typed RPC per soup ruling |
| ◐ | search_service | `crates/search_service` (7,061 LOC / 31 files); routers `crates/search_service/src/api/search/mod.rs:19-21`, `api/search/simple/mod.rs:21`; per-entity handlers `api/search/{document,project,chat,channel,email,call_record,crm_company}.rs` | **Tiny HTTP surface, wide entity coverage.** Only 3 mounts — `POST /` (unified), `POST /simple` (a reduced/faster variant with its own per-entity handlers under `api/search/simple/`), and `/channel` — but each fans out across **7 indexed entity types**: document, project, chat, channel, email, call_record, crm_company. Also `terms.rs` (query-term parsing) and `enrich.rs` (result hydration from Postgres after the index returns ids). Backend is **OpenSearch** (`infra/stacks/opensearch`), which the index is queried against directly; `search_service` owns query construction + enrichment, **not** indexing (that is `search_processing_service`). | Defer | Query/enrichment layer is a thin RPC surface under **R3**; the substrate choice is the real decision and it is a **D1/1a index-layer** question, not a separate product — the ruled "required materialized-index layer" and the search index are the same architectural slot and should be designed together, not as two stores. Candidates: D1 FTS5 for lexical + Vectorize for semantic, with enrichment reading the entity DOs. The 7 entity types are the coverage contract. Effort: small-to-medium for the query surface; the substrate is the cost. **Rule with `search_processing_service`** — they are one system. | — |
| ◐ | properties | `crates/properties` (20,911 LOC / 53 files — **the largest domain crate in the ledger**); router `crates/properties/src/inbound/axum_router.rs:115-180`; schema `crates/macro_db_client/migrations/20251030100000_init_properties_schema.sql:5-178`; toolset `crates/properties/src/inbound/toolset/` | The EAV domain surface (storage direction already fixed by **D2/2a**). **19 endpoints in four groups**: `/definitions` (+`/{id}`, `/{id}/options`, `/{id}/options/{option_id}`), `/tags` (+`/tags/promote`, `/tags/merge` — tag lifecycle is first-class), `/entities/{entity_type}/{entity_id}` (+`/{property_id}`, `/options/{option_id}`, `/options/bulk`, `/entities/bulk`, `/options/bulk`), `/entity_properties/{id}`. **Bulk is pervasive** — four of the 19 are bulk paths, which is a load-bearing shape for grid/kanban editing, not an optimization. **9 agent tools** (`set_entity_property`, `bulk_set_entity_property_options`, `get_entity_properties`, `list_tags`, `create_tag`, `edit_tag`, `delete_tag`, `tag_color`). Domain carries its own `activity/` and `events/` trees — the side-effect pipeline. 3 tables (`property_definitions`, `property_options`, `entity_properties`) + 2 enums: `property_data_type` (9: BOOLEAN, DATE, NUMBER, STRING, SELECT_NUMBER, SELECT_STRING, ENTITY, LINK) and `property_entity_type`. Publishes to `macro.properties`. | Keep | Semantics kept exactly per **D2/2a**; values on the entity record; filter-index layer first-class (**D1/1a**). Surface = RPC namespace under **R3** preserving the four groups and, critically, the **bulk shapes** (a per-item RPC loop is not a faithful recreation of grid editing). Side-effect pipeline gets **pattern-3 treatment (D3/3a)** per the D2 rider — property writes fan out to activity + `macro.properties` consumers, so intent record + in-DO alarm + idempotent consumers. 9 tools → Gatekeeper session APIs (frozen ai_toolset). **The D2 entity-type-canonicalization rider is blocking and now has exact evidence:** `property_entity_type` holds **10** values (CHANNEL, CHAT, DOCUMENT, PROJECT, THREAD, USER, then COMPANY/TASK `20251128000000_add_system_properties.sql:3-4`, CALL_RECORD `20260709192942:2`, CALENDAR_EVENT `20260726023229:3`) against `EntityType`'s **16** — and `THREAD`/`TASK` exist here with **no `EntityType` variant** because both are document facets. Canonicalization cannot be settled without deciding whether thread and task are entities (see `audits/documents-audit.md` §F). Effort: large. | — |
| ✔ | crm | `crates/crm` | Companies/contacts CRM — **audited 2026-08-19**: mounted at `/crm` in DSS; email-traffic-derived (populate/depopulate off `email_links`); domain-keyed companies per team; EAV Stage/Owner/Revenue (companies only, contacts have none); global Apollo-backed domain directory; hidden/email_sync/killswitch machinery. Full audit: `audits/crm-audit.md`. | Keep | DO/D1 | **Keep, in the pilot (2026-08-19).** What the new CRM actually contains is decided by David + agent at design time from what the source really does. |
| ✔ | call | `crates/call` | Calls (LiveKit) | ~~Drop~~ | LiveKit stays external; call domain on DO/D1 | **Keep (2026-08-19)** |
| ◐ | favorites | `crates/favorites` (1,531 LOC / 11 files); router `crates/favorites/src/inbound/axum_router.rs:85-108`; table `crates/macro_db_client/migrations/20260702014623_create_favorites_table.sql:2`; cap `crates/favorites/src/domain/service.rs:17`; hydration join `crates/favorites/src/outbound/pg_favorites_repo.rs:153-158`; also `crates/graphql_favorite` | A user's personal **ordered** collection of any entity. 4 endpoints (`GET`/`POST /favorites`, `DELETE /favorites/{entity_type}/{entity_id}`, `PATCH /favorites/reorder`). One table `favorite`, PK `(user_id, entity_type, entity_id)` — **no surrogate id**; `sort_order DOUBLE` = fractional-index reordering; two indexes (ordered listing per user; reverse "is this favorited?"). Cap **500 per user**, enforced on add *and* reorder. **Publishes no events** (nothing Kafka-facing in the crate). Adding requires an `EntityAccessReceipt<ViewAccessLevel>`; **listing re-checks nothing** — `list_favorites` (`service.rs:103-112`) and the handler (`axum_router.rs:166-180`) return the raw rows, so a revoked entity stays visible until removed. Listing hydrates display metadata via a **6-way LEFT JOIN** over `Document`, `document_sub_type`, `Chat`, `Project`, `comms_channels`, `comms_messages` — the D1 glue at its most literal. Display *names* are deliberately not stored (resolved from the previews pipeline so renames and viewer-relative DM names stay correct). `favorited_entities` decorates Soup rows. CRM entities are **not** favoritable at the pin (`audits/crm-audit.md` §D12). | — | Per-user DO collection under **D1/1a** — the best fit in the ledger for the kernel's per-user DO idiom (`[CF] packages/workshop-backend/src/user.ts:151-220`): small, per-user, ordered, read on every page. Fractional `sort_order` ports unchanged. The **6-way hydration join is the whole design question**: it becomes either a batch read against the entity registry or a denormalized projection maintained by the **D1/1a materialized-index layer**. The read-side access gap is a decision, not a bug to copy — it interacts with the ruled share-permission invariant ("SEC holes fixed, not recreated"). Effort: **small** — the smallest genuine row in the ledger. | — |
| ✔ | reminders | `crates/reminders` | Reminders | ~~Drop~~ | DO alarms / kernel scheduler | **Keep (2026-08-19)** |
| ◐ | foreign_entity | `crates/foreign_entity` (3,348 LOC / 12 files); router `crates/foreign_entity/src/inbound/axum_router.rs:81-96`; models `src/domain/models.rs:44-84`; table `crates/macro_db_client/migrations/20260526175912_create_foreign_entity_table.sql:1` | A **live reference** to a record that stays in an external system — explicitly the opposite of `import`, which copies (`crates/import/src/lib.rs:1-14`). Row = `{id, foreign_entity_id, foreign_entity_source, metadata JSONB, stored_for_id, stored_for_auth_entity}`; the `stored_for_*` pair is a **second** polymorphic scope (`user` or `team`, `models.rs:26-34`) layered on the entity glue. **One public endpoint** (`GET /foreign_entity/{id}`); everything else is in-process. **Exactly one writer and one source value in practice:** GitHub PR sync writes `"github_pull_request"` (`crates/github/src/domain/service/sync/mod.rs:596-598`; constant at `crates/github/src/domain/models/pull_request.rs:8`). It has **no sync engine of its own** — freshness is entirely GitHub's webhook push. `EntityType::ForeignEntity` is one of the 16 canonical variants, so these rows are first-class Soup items with their own filter literal (`crates/item_filters/src/ast/foreign_entity.rs`, filtering by `foreign_entity_sources`) and their own frontend block (`pr`). Read by documents, memory, soup, ai_tools. | — | A thin generic table today; under **D1/1a** it is the cleanest case of an entity that exists *only* in the registry (id, type, tenant, tombstone) with its payload in `metadata`. Two shape questions: whether "live reference" survives as a concept at all (`audits/connectivity-layer-audit.md` §D5 frames copy-in vs live-reference — that ruling decides this row), and whether the `stored_for` user/team scope collapses into the kernel's ownership model. Because it has exactly one producer, **it should be ruled with `github`**; on its own it is an empty container. Effort: small; near-zero if the GitHub row is dropped. | — |
| ◐ | webhook (**outbound**, not ingestion) | `crates/webhook` (11,842 LOC / 31 files; 6,848 excl. tests); router `crates/webhook/src/inbound/axum_router.rs:202-215`; retry policy `src/domain/delivery.rs:17-22`; signing `src/outbound/http_delivery.rs:27,48,98-128`; SSRF guard `src/outbound/http_validator.rs:157,243`; ingestion `src/domain/ingestion.rs:226-322`; tables `crates/macro_db_client/migrations/20260629135403_webhooks_core_schema.sql:1,30,69` | **The ledger's "webhook ingestion" label is wrong — this is an outbound delivery engine**, i.e. Macro-as-webhook-*provider*. 6 endpoints (`GET`/`POST /webhook/webhooks`; `GET`/`PATCH`/`DELETE /webhook/webhooks/{id}`; rate-limited `POST /webhook/webhooks/{id}/validate`). **3 tables:** `webhook` (endpoint, `signing_secret`, custom `headers JSONB`, `rule JSONB` event filter with a **GIN index on `rule->'events'`**, status `active`\|`paused`\|`disabled`, `is_valid`, pause reason, last success/failure), `webhook_delivery` (**`UNIQUE(webhook_id, event_id)` = idempotency**, `event_ordering_key`, `next_attempt_at`, `attempt_count`, redacted request headers), `webhook_delivery_attempt` (per-attempt status, duration, error kind, body preview). **Delivery policy is exact:** max **5** attempts, fixed backoff **30s / 60s / 120s / 300s**. **Signing:** `x-macro-signature` HMAC over timestamp + raw body, plus `x-macro-event`, `x-macro-event-id`, `x-macro-timestamp`; reserved `x-macro-*` headers cannot be overridden by user config. **Subscribable catalog** (Kafka-fed; `WebhookEvent` at `src/domain/events.rs:196`): **5 of the 8 document events** (`created`/`updated`/`deleted`/`copied`/`interaction` — `content_uploaded`, `sync_content_updated`, `purged` are deliberately **not** forwarded, `ingestion.rs:234-236`), **11 channel events**, and **4 meta events** about webhooks themselves. Owner is a user **or a bot** (`owner_user_id`/`owner_bot_id`) — so this and `bots` are one product surface. | — | **R3** puts CRUD on `/api` RPC; delivery is a background job, not a route, so **C2's `/hooks/<source>/*` is inbound-only and does not apply here**. Under **D3/3a** the delivery row is exactly the ruled intent-record shape — `UNIQUE(webhook_id, event_id)` already provides the idempotency the ruling demands — and the retry ladder becomes a DO alarm; under **D4/4a** the drain is a single-writer DO per webhook, which also preserves `event_ordering_key`. **The blocker is the SSRF guard:** `validate_resolved_endpoint_url` resolves DNS and rejects private/loopback IPs, and **Workers cannot resolve hostnames** — see `audits/standalone-services-audit.md` §C1, one ruling covering this row plus unfurl, image-proxy and connector endpoints. Rule with `bots` (shared ownership model). Effort: **medium-large** — the policy is small; the delivery guarantees are the work. | — |
| ◐ | bots (+ `channel_bots`) | `crates/bots` (6,575 LOC / 16 files) + `crates/channel_bots` (1,123 LOC); routers `crates/bots/src/inbound/axum_router.rs:130-164`, `channel_webhook_router.rs:188,207`; tokens `crates/bots/src/domain/tokens.rs:5-15`; events `src/domain/events.rs:78-86`; tables `crates/macro_db_client/migrations/20260527160000_channel_bots.sql:1,35`; already-CF examples `services/bots/anthropic-status-bot`, `services/bots/stripe-payment-bot` | **Non-human principals that participate in channels.** 14 endpoints: bot CRUD, `GET /bots/me` under bot-token auth, bot↔channel membership, token list/create/revoke, `POST /channels/{id}/bots/scoped`, and the **unauthenticated** `POST /channels/{id}/webhook` poster. 2 tables: `bots` (`kind` = `owned`\|`system`, CHECK that `owned` has **exactly one** of `owner_user_id` XOR `team_id` and `system` has neither; `handle`; soft delete) and `bot_tokens` (`token_hash bytea` + indexed `token_prefix`, `expires_at`, `revoked_at`, `last_used_at`). **Token format `mbot_<12-hex-prefix>_<64-hex-secret>`** — prefix indexed, secret hashed, URL-path-safe by test. **3 events** (`bot.created/updated/deleted`). `channel_bots` is the other half: channels emit a `ChannelBotTrigger` when a message **mentions** a bot; **system** bots run in-process, and the only one is **Macro AI**, which posts a "thinking" message, runs the agent loop, then edits that message with the answer. `bots` is also the second owner type on the `webhook` row. Frontend: `bots` settings tab behind the `bot-management` flag (EF §6). | — | Two separable halves. **(a) Bot identity + tokens** is an auth-model question belonging with the ruled auth rebuild — a bot is a principal with scoped credentials, which is what the kernel expresses as gatekeeper/agent identity; the platform's own `actor_id` convention already prefixes principals `macro`\|`…` / `bot`\|`…` (see the DSS-native chrome row). **(b) Mention-triggered in-channel agents** is a kernel-session shape: mention → session → post/edit, i.e. **C1** session events over the ruled kernel agent runtime, not a new runtime. **Finding for the lift discussion (A3):** the two `services/bots/*` workers are **already Cloudflare Workers** (`wrangler.jsonc`, no AWS dependency) and are the only working reference for the `x-macro-bot-token` / `x-macro-bot-scope` webhook contract — `audits/standalone-services-audit.md` §G. Effort: medium; the identity half is gated on auth design. | — |
| ◐ | github | `crates/github` (17,523 LOC / 36 files); router `crates/github/src/inbound/github_sync_router/mod.rs:77-85`; webhook dispatch `src/domain/service/sync/mod.rs:907-995`; OAuth `src/outbound/github_oauth_client.rs:34-88`; notification helpers `src/domain/service/sync/notify_pr*.rs`; tables `20260226142003` (`github_links`), `20260305182148` (`github_pr_tasks`), `20260527141944` (`github_app_installation`), `20260708172449`, `20260804132220` | **The only external system with a real bidirectional sync at the pin.** 3 DSS routes (`GET /github/install-sync`, `GET /github/sync-redirect`, `POST /github/webhook`) plus 4 on the auth service (`POST`/`DELETE /link/github`, `GET /link/github/status`, `POST /github_pull_requests/enrich`). **Two independent identity mechanisms:** a per-user OAuth identity link (`github_links`, hand-rolled OAuth) and **GitHub App installations** — the only *team-scoped* external connection in the platform (`github_app_installation` with `source_type` \u2208 `team`\|`user`, plus installer and pending-request tables). **6 webhook event types handled** (`sync/mod.rs:907-995`): `pull_request` (actions opened/reopened/edited/closed), `issue_comment`, `pull_request_review`, `pull_request_review_comment`, `check_run`, `installation` (created/deleted); unknown types are skipped. Every PR-associated event **upserts `foreign_entity` rows** (`sync/mod.rs:596`) — that is what makes a PR a first-class Soup item and the `pr` block. **7 notification kinds** are emitted from here (status transitions, review requested, PR-body mentions, comment + mentions, review submitted, check-run, plus the shared common shape) — the 7 `github_pr_*`/`github_review_requested` types on the `notification_service` row, i.e. **GitHub alone is 7 of the platform's 19 notification types**. Task coupling: `github_pr_tasks`, `GET /documents/{id}/github_prs`, and `GET /documents/{id}/branch_name` (`audits/documents-audit.md` §A1/§D2). | Keep (verifier path) | Under the ruled model this is a **gatekeeper** (`gatekeeper-github` already exists in the kernel fleet and is the registration reference cited in `cloud-handoff.md` §5), plus **C2 `/hooks/github/*`** for the webhook ingress, plus a `foreign_entity`-shaped mirror under **D1/1a**. Three things do **not** come free from the existing gatekeeper and are the real scope: (a) the **team-scoped App-installation** model, which has no analogue in the kernel's per-user account grants (`audits/connectivity-layer-audit.md` §C3 records team-credential sharing as an open gap); (b) the **PR→entity mirror + Soup/block surfacing**; (c) the **7 notification types**, which are only meaningful if the notification row survives. Rule **with `foreign_entity`** and after the notification ruling. Effort: **large** — the third-biggest crate in the ledger after `properties` and `documents`. | — |
| ✔ | calendar_events / cal | `crates/calendar_events`, `crates/cal` | Calendar (sync coupled to email accounts) | ~~Drop~~ | Workers sync + DO/D1 | **Keep (2026-08-19)** |
| ✔ | sync_service (domain) | `crates/sync_service` + `services/sync-service` | Loro CRDT sync (already CF) | — | Lift as-is | **Lift (2026-08-19)** — see §3 CF-services ruling |
| ◐ | DSS-native: activity, pins, recents, history, annotations, saved-views, entity-access | `services/document_storage_service/src/api/{activity,history,pins,recents,annotations,saved_views.rs,threads,entity,user_document_view_location,instructions}` + `crates/entity_access` (62 files, **6,448 non-test LOC** + 13,519 test LOC), `crates/entity_access_management` (1,225), `crates/entity_access_db_utils` (1,057), `crates/activity` (893), `crates/frecency` (4,450), `crates/saved_views` (235); activity table `crates/macro_db_client/migrations/20260805180315_create_activity_events.sql:4,21,23`; action vocabulary `crates/activity/src/domain/models.rs:102`; frecency constants `crates/frecency/src/domain/models.rs:199-205,308,314-315` | **Reads like chrome; is not.** Full audit: `audits/dss-native-chrome-audit.md`. Six of the seven names are genuinely small (annotations 1,611 LOC/8 endpoints; saved-views 307+235/5; pins 258/4; history 249/3; activity 102+893/1; recents 64/1 — and `recents` is only `GET /recents/deleted`, *not* the recents users see). The seventh, **`entity_access`, is the platform's authorization core**: 13 per-entity-type access-query modules (document, project, chat, thread, channel membership/role/users, call, CRM company, CRM contact, foreign_entity, team) and **14 typed axum extractors** that make "this handler needs Edit on a document" a compile-time obligation via `EntityAccessReceipt<L>` capability markers (View/Comment/Edit/Owner + role variants). **68% of the crate is tests** — the only executable specification of the ruled share-permission invariant, and where the SEC-1/2/3 holes live. `activity_events` is an **append-only fact log with uuidv5 ids** (replays are idempotent by construction), a **closed 10-action vocabulary** whose doc comment states renaming a variant is a storage migration, an **actor vs subject** split (`on_behalf_of ?? actor` — so agent actions are attributable to both agent and person), and two keyset indexes = exactly two surfaces (my-activity, entity-activity). **`frecency`** is the real recents/quick-access engine — score = `0.7×frequency + 0.3×recency`, exponential decay `0.1`/hour, last 10 events retained — and is consumed by soup, memory, ai_tools, email and channels **with no ledger row of its own**. Saved views exist in three tiers (personal server rows, team blob in `team_crm_settings.team_views`, base64url share links) and the `config` JSON is **opaque to the backend in all three**. | — | Split, not one target. **entity-access** is the concrete form of **D1/1a**'s "per-type access policy stays code" and is a prerequisite for every other kept domain — it is ruled first or everything else is blocked. **activity** is the cleanest **D3/3a** fit in the long tail (already idempotent, already keyed for replay); its 10-action vocabulary is a portable contract, and it overlaps the `macro.*` topic list on the `search_processing_service` row. **pins/history/view-locations/saved-views/favorites** are per-user state and are the most natural fit in the ledger for the kernel's per-user DO (`[CF] packages/workshop-backend/src/user.ts:151-220`). **annotations** is the same build as `documents` (`audits/documents-audit.md` §D1) and should move with it. **frecency** and the **activity_events vocabulary** need rows that do not exist today. Effort: **entity-access large; everything else small** — audit §H offers three splits (N1 four-way, N2 entity-access only, N3 one row with sub-scope). | — |

## 2. Backend domains — DCS composition root (`/cognition`)

| St | Capability | Old source | What it is | Provisional | Proposed CF target | Verdict |
|---|---|---|---|---|---|---|
| ✔ | chat | `crates/chat` | Chat threads/messages + streaming | Keep | Faithful UX over kernel sessions | **Kernel runs, old UX on top (2026-08-19)** — chat experience recreated as surfaces over kernel sessions |
| ✔ | agent | `crates/agent` | Agent runtime | Replaced | CF-OS kernel agent loop | **Kernel is the one agent runtime (2026-08-19)** |
| ✔ | ai_tools / ai_toolset | `crates/ai_tools`, `crates/ai_toolset` | Tool defs; **frozen ai_toolset invariant** | Keep frozen | Gatekeeper session APIs | **Semantics recreated as Gatekeeper session APIs, mapped piece-by-piece factually from source (2026-08-19)** |
| ◐ | memory | `crates/memory` (1,076 LOC / 12 files); router `crates/memory/src/inbound/axum_router.rs:67`; staleness `src/domain/service.rs:104`; prompt + extraction `src/domain/service.rs:20-51,156-242`; toolset wiring `src/context.rs:99-235`; standalone bin `src/main.rs`; table `memory` (`20260323000000`, SH §1.1) | **One prose blob per user, not a memory store.** A "memory" is a **~1000–3000 word summary of the user** prepended to future AI prompts (`service.rs:51`), held in `memory(id, user_id UNIQUE, memory TEXT)`. **One endpoint**, `GET /memory`, with stale-while-revalidate semantics: fresh → return; **older than 24h** or missing → trigger background generation and return the stale copy (200) or 404 if none exists. Generation is **a full agent session over the user's workspace**: `crates/memory/src/context.rs` wires the entire `all_tools` set (documents, email, properties, call, chat, skill, soup, frecency, foreign_entity) into the run, and the model must wrap its answer in `<memory>` tags for extraction (`service.rs:221-242`); a previous memory is passed in as the baseline for incremental refinement. There is also a **standalone CLI binary** (`USER_ID` env) for out-of-band generation. Metered as `AiFeature::Memory` on the `ai_usage` row. Consumed by chat prompts and by `scheduled_action`'s agent runs. | — | Under the ruled model this is not "agent memory" in the kernel sense (per-session context) — it is a **periodically-materialized, agent-generated user profile**, structurally the same pattern as the `ai_projections` row: prompt + cadence + cached result. Both should be looked at together; a single "materialized agent output" primitive would cover them. CF-native: the blob is a per-user DO field; the 24h staleness + background regeneration is a **D4/4a** per-user alarm (no dispatcher); the generation run is a kernel agent session with a **read-only** toolset. The open product question is whether a whole-workspace agent sweep per user per day is affordable and desirable at all — it is one of the most expensive recurring AI costs in the old system. Effort: small to rebuild, large to *decide*. | — |
| ◐ | import | `crates/import` (7,423 LOC / 18 files); router `crates/import/src/inbound/axum_router.rs:73-81`; source model `src/domain/models.rs:32-98`; gather service `src/domain/service.rs:1-57,448-511`; prompts `src/domain/service/prompts.rs`; staging tool `src/inbound/toolset.rs`; gateway push `src/outbound/gateway_notifier.rs`; tables `crates/macro_db_client/migrations/20260720221050_import_entities.sql:5,35` + `20260723150434` | **Copy-in, the counterpart to `foreign_entity`'s live-reference** (`src/lib.rs:1-14`). A staging ledger: `import_entity` moves `staged` \u2192 `importing` \u2192 `imported` \| `discarded`, `import_run` tracks one gather job per `(user, source)`. **4 endpoints**: `GET /import/state`, `POST /import/run`, `POST /import/runs/{source}/retry`, `POST /import/runs/{source}/dismiss`. **`ImportSource` is a hardcoded 3-variant enum**, each pinned to a **hosted MCP server URL** and a **fixed** target entity type (`models.rs:52-70`): Linear → `https://mcp.linear.app/mcp` → `task`; Notion → `https://mcp.notion.com/mcp` → `md`; Slack → `https://mcp.slack.com/mcp` → `channel`. The comment is explicit: *"importers have latitude on content, never on shape."* Per-source foreign-id normalization is real logic (Notion URLs collapse to the 32-hex page id so ids survive renames; Slack ids `C0123ABCD…` pass through while names normalize to `#name`) — `models.rs:76-97`. **Gathering is a cheap agent session** (24-turn cap, 90s timeout, fast model) whose toolset is the user's connector `McpToolSet` plus an in-process `CreateImportEntity` tool locked to `(user, source, initiator)`, so the model stages candidates by tool call with **no structured-output parsing**; Slack has a deterministic direct-API fallback. Runs are auto-started by the onboarding read loop and by the post-OAuth hook (see `onboarding` row). Metered as `AiFeature::Import`. Full analogue analysis: `audits/connectivity-layer-audit.md` §A4. | — | Named in `audits/connectivity-layer-audit.md` §C8 as **the strongest existing analogue of the ruled P1 connectivity centerpiece** — "pull external data into the workspace" — and the kernel has nothing like it. Under the ruled model: the ledger becomes DO state; the gather job is a kernel agent session with a locked staging tool; the 3 hardcoded sources become gatekeeper/MCP connections (`gatekeeper-mcp` already handles hosted MCP endpoints, so the *transport* is free and the **source→entity-type mapping and normalization are the actual harvest**); progress push moves from connection_gateway to **C1** kernel session events; run lifecycle is a **D4/4a** per-run DO alarm, not a poller. The open ruling is `audits/connectivity-layer-audit.md` §D5/§D7: does copy-in survive alongside live-reference, and does the cheap-gather-agent pattern port as the Instantly ingestion mechanism? Effort: medium — and it is **P1-adjacent scope**, not tail scope. | — |
| ◐ | onboarding | `crates/onboarding` (870 LOC / 10 files); router `crates/onboarding/src/inbound/axum_router.rs:69-70,80-129`; service `src/domain/service.rs:26-30,104-194`; repo `src/outbound/pg_onboarding_repo.rs:46-63`; mounted on **DCS** `services/document_cognition_service/src/api/mod.rs:107-112`; post-OAuth hook `services/document_cognition_service/src/main.rs:618-633`; table `user_onboarding` (`20260720224652`); frontend `apps/web/src/features/setup/flow/`. Full mechanics: `business-chrome-primitives.md` §2 | **Two endpoints and one row — the flow itself is frontend.** `GET /onboarding` returns an aggregate `OnboardingState` (the `user_onboarding` row + the user's MCP connector connections + `suggested_team_domain`, which is the email domain unless it is a generic consumer provider — the same judgment the teams service uses for domain auto-join). **Reading the state is what drives the flow:** while the row is `active`, each read starts any due import gathers (one auto-import per authenticated connector that never had one, CAS-protected); the frontend polls it **every 12s**. A post-OAuth hook reconciles instantly and is a no-op for users not in the flow. `POST /onboarding/complete` marks the row completed (idempotent, records `skipped`) and best-effort **deletes** unreserved onboarding-staged import candidates — deleted, not "discarded", so they stay re-stageable. Note the real app-entry gate is **not** this row: it is the legacy `User.tutorialComplete` boolean flipped by `PATCH /user/tutorial` on the auth service. `PATCH /user/onboarding` (auth service) is a plain profile write despite the name. Two generations coexist behind a PostHog flag (interactive tutorial modal vs the full-screen v4 flow). | — | **Parked-prepare** by the 2026-08-19 Q19 ruling (business chrome — David plans something different); this row is the *mechanical* record for that redesign. The backend surface is genuinely tiny (2 endpoints, 1 row), so almost nothing here is a port. The two pieces with carry-over value are (a) **read-triggers-work** — the polling `GET` that starts gathers is a substitute for the push the kernel session gives for free (**C1**), and (b) the **connector-first flow shape**, which is the ruled P1 connectivity layer's own onboarding and is therefore likely to be re-derived rather than recreated. The `tutorialComplete`-vs-`user_onboarding` **dual gate is a bug to not recreate**. Effort: negligible as a port; the scope is whatever David's replacement flow needs. | — |
| ◐ | ai_usage | `crates/ai_usage` (1,096 LOC / 10 files); router `crates/ai_usage/src/inbound/axum_router.rs:98-99`; feature vocabulary `src/domain/ports.rs:42-63`; pricing/compute `src/domain/ports.rs:69-82`; service `src/domain/service.rs:30-113`; tables `ai_usage` (`20260616194726`), `ai_pricing` (SH §1.9) | Token/cost metering. **2 admin-only endpoints** (`POST /ai-cost/usage` to query, `POST /ai-cost/pricing` to set a model's price **and recompute existing rows**), both 403 for non-admins. `ai_pricing` is keyed by model string with `price_per_million_in/out REAL`; `ai_usage` rows carry `{feature, user_id, entity, model, input_tokens, output_tokens, total}`; cost is computed **at write time** from the then-current price, which is why re-pricing has to recompute. Queries roll up by feature. **The `AiFeature` enum is the single most reusable artifact here** — it is the complete inventory of AI-spend surfaces in the old platform (10): `Chat`, `Memory`, `Automation`, `DynamicCompletionsApi`, `ChatRename`, `CallSummary`, `ChannelBot`, `AiProjection`, `AiEditing`, `Import`. Every one of those maps to another ledger row, so the enum doubles as a checklist of which capabilities cost money per use. | — | Cloudflare **AI Gateway** provides request logging, token counts and cost analytics natively, so the *collection* half largely dissolves; what does not dissolve is the **`feature` dimension** — AI Gateway sees models and requests, not "this was a memory regeneration". Carrying the 10-value feature tag through as a gateway request attribute (or a small D1 rollup table) is the actual work, and it is what makes per-feature cost visible. Interacts with the model layer (SUP-536 / OpenRouter, `docs/sup-536-openrouter-changes.md`): whoever routes model calls is the natural place to stamp the feature tag. Also note the billing row's entitlement model is **role-driven, not usage-driven** (`business-chrome-primitives.md` §1.1), so this table never gated a user — it is cost observability, not metering-for-billing. Effort: small. | — |
| ◐ | ai_projections | `crates/ai_projections` (3,549 LOC / 21 files); router `crates/ai_projections/src/inbound/axum_router/mod.rs:59`, paywall check `upsert_projection.rs:108-111`; cadence enum `src/domain/model.rs:22-28`; queue/worker `src/domain/ai_projection_queue.rs`, `src/worker.rs`, `src/outbound/sqs_queue.rs`; generator `src/outbound/agent_generator.rs`; push `src/outbound/gateway_notifier.rs`; tables `ai_projection`, `user_ai_projection`, `processing_ai_projections` (SH §1.9); refresh Lambda `infra/stacks/document-cognition-service/ai-projections-refresh-trigger.ts:18-20` | **A registered prompt + output schema, materialized per user or team and refreshed on a cadence.** `ai_projection` holds `{prompt, prompt_hash, target_type user\|team, refresh_cadence, expiry, model, output_schema}`; `user_ai_projection` holds the materialized result per target with status `loading`\|`cold`\|`ready`\|`refreshing`\|`error`; `processing_ai_projections` is an **insert-as-lock** with a blanket 15-minute DELETE reclaim — the weakest lease tier catalogued in `pattern-review.md` §4. **1 endpoint** (`POST /ai-projections`, get-or-create projection + user instance), gated on `read:professional_features` for premium models. **3 cadence tiers** (`high` 6h, `medium` 1d, `low` 3d) fired by three EventBridge rules against one handler (`audits/lambda-batch-families-audit.md` §F). Generation runs an agent session (`agent_generator.rs`) and results are pushed to the client through connection_gateway. Metered as `AiFeature::AiProjection`. Product surface: **Home recommendations** (`features/home/home-hub.tsx`, `createHomeRecommendations.ts`) — the AI-suggested prompts and attachments on the landing view — plus a dev-only projection playground. | — | Structurally the **same primitive as `memory`**: prompt + cadence + cached structured result. Rule them together; one "materialized agent output" concept covers both, and `/structured-completion` (streaming/completions row) is its synchronous form. CF-native: definition rows are small read-mostly team/user data; the instance is a per-target DO field; the three cadence tiers become **D4/4a** per-target alarms (the 3 EventBridge rules and the insert-as-lock both disappear — the ruling explicitly does not port claim tables or dispatchers); status transitions and the client push become **C1** kernel session events. `output_schema` (a `DynamicSchema`) maps onto the kernel agent's structured-output support. Open question for David: does the Home-recommendations surface exist in an outreach product at all — the whole row is downstream of that. Effort: medium. | — |
| ◐ | mcp_client | `crates/mcp_client` | MCP client — audited 2026-08-19: Streamable-HTTP only via rmcp, per-user servers, AES-GCM creds, 3-strategy OAuth (pre-reg/CIMD/DCR), searchable tool catalog (schemas never in prompt), CRUD at `/mcp/servers*` in DCS. See `audits/connectivity-layer-audit.md` §A1. | Replaced | `gatekeeper-mcp` (+ mcp-shared covers most of this already) | — |
| ◐ | streaming/completions (native) | DCS root: `services/document_cognition_service/src/api/stream/` (1,169 LOC — `chat_message/mod.rs`, `stop.rs`, `util/{chat_message,chat_permissions}`), `api/structured_completion.rs` (179 LOC), `api/completions.rs` (47 LOC); mounts `api/mod.rs:84-90`; durable stream `crates/stream` (1,696 LOC, `src/domain/traits.rs:11-48`, `src/outbound/redis_pg/`); cancellation registry `services/document_cognition_service/src/service/ai_stream_registry.rs:7,41,66`; table `active_streams` (`20260217194926`) | **4 endpoints, three different mechanisms.** (1) **`POST /stream/chat/message` does not stream to its caller.** It returns a `stream_id`; the client then subscribes over connection_gateway (`chat_message/mod.rs:127`). The transport underneath is a **durable, offset-addressed, resumable stream**: `StreamRepo` supports `append`, `stream_from_beginning` (full replay), `close`, `active_streams(entity)` and a lifecycle `notify()` channel, with a **30-minute default lifetime** (`traits.rs:11-12`) over a Redis+Postgres adapter. (2) **`POST /stream/chat/message/stop`** fires a `CancellationToken`, published **over Redis pub/sub so it reaches whichever DCS instance is running the loop** (`stop.rs:97-105`); the running task then persists what the user already saw and emits `StreamEnd`. (3) **`POST /structured-completion`** = `{prompt, model, output_schema: DynamicSchema, additional_instructions?, toolset}` → one AgentLoop → JSON result; it is the synchronous sibling of `ai_projections`. (4) **`POST /chat/completions`** is a **47-line hardcoded non-streaming proxy to `https://api.openai.com/v1/chat/completions`** using the platform's own OpenAI key, with `stream` forced to `false`. Premium-model access is checked here via `ChatModelAccess` (`business-chrome-primitives.md` §1.1). | Keep | The durable-stream half is **the strongest "the kernel already has this" case in the ledger**: resumable offset-addressed streams, replay-from-beginning, lifecycle events and cross-instance cancellation are exactly what a DO-hosted session provides natively, so `crates/stream` + Redis + `active_streams` + the pub/sub cancel registry all **dissolve** rather than port — consistent with the ruled "kernel is the one agent runtime" and **C1** (stream lifecycle `created`/`closed` was already enumerated on the connection_gateway row as a session-native event family). `/structured-completion` becomes an RPC method under **R3** over the kernel agent's structured output. **`/chat/completions` is the one clear kill candidate in this row**: a bare OpenAI passthrough is superseded outright by the model layer (SUP-536 / OpenRouter, `docs/sup-536-openrouter-changes.md`) — but it is an *external* contract shape, so it should be checked for callers before removal. Effort: small — most of it is deletion. | — |

## 3. Standalone services & processes

| St | Capability | Old source | What it is | Provisional | Proposed CF target | Verdict |
|---|---|---|---|---|---|---|
| ✔ | authentication_service | `services/authentication_service` | Real multi-tenant auth (`/auth`, ~79 endpoints): sessions, signup/login, OAuth, teams, invites, permissions; FusionAuth backend; Stripe webhooks ride on it | ~~Replaced by Access~~ | Workers + DO/D1; identity backend chosen at design time | **Rebuild real auth Cloudflare-native (2026-08-19).** FusionAuth stays dead; Access is not the auth model. |
| ✔ | connection_gateway | `services/connection_gateway`; shared types `crates/connection_gateway_models/src/lib.rs:1-62`; publisher clients `crates/connection_gateway_client/src/client/{email,calendar}.rs`; protocol schemas `apps/web/src/lib/service-clients/service-connection/openapi.json` | WebSocket fan-out — WS upgrade at literal `GET /` (`src/api/connection/mod.rs:34`), JSON protocol (track_entity/stream_events, carries AI stream + email refresh events). **Event types enumerated 2026-08-19 (research pass) for the C1 kernel-session design.** *Transport:* 3 HTTP paths — `GET /track/{entity_type}/{entity_id}` (the WS upgrade), `POST /message/send/{entity_type}/{entity_id}`, `POST /batch_send`. *Client→server* (`ToWebsocketMessage`, 2 variants): `track_entity` — carries `TrackAction` = `open`\|`ping`\|`close`, i.e. **presence with an explicit heartbeat**; and `stream_events` — subscribe to AI streams for an entity. *Server→client:* an entity-addressed envelope with a **free-form `message_type: String`** plus JSON payload (`UniqueMessage`/`SendMessageBody`), so the type set is defined by publishers, not by an enum. Types found at the pin: **`refresh_email`** (`connection_gateway_client/src/client/email.rs:15`), **`refresh_calendar`** (`client/calendar.rs:15`), **`user_tracking_change`** (presence fan-out), **`StreamEvent`** = `created`\|`closed` (AI stream lifecycle, keyed by `StreamId`, payloads as `StreamItem{id,payload}`), and **`UploadFolderStatusUpdate`** = `completed`\|`partially_completed`\|`failed`\|`unknown` (the projects-row folder-upload progress channel). Delivery is receipted: `MessageReceipt{user_id, delivery_count, active}`. Addressing uses the full 16-variant `EntityType`. **Note the two shapes are different problems:** presence/tracking and stream lifecycle are session-native; `refresh_email`/`refresh_calendar`/upload-status are **cache-invalidation pokes** ("something changed, refetch"), which is a different design question for the kernel session. | Replaced | DO hibernatable WebSockets; `route-reconciliation.md` §5 proposes supersede-by-kernel-`/api`-session (ruling C1). Design inputs now available: the 2 client→server verbs, the 5 server→client type families above, the `open/ping/close` presence contract, and the receipt shape. The free-form `message_type` should become a **closed union** in the kernel session events — the string was the source of the enumeration difficulty. `document.interaction` (`audits/documents-audit.md` §C) belongs with these session events rather than the D3 durable outbox. | **Superseded by kernel `/api` session push (2026-08-19, ruling C1)** — its event types become kernel session events at domain design time; no dedicated `/ws` service |
| ✔ | email_service (company mailbox) | `services/email_service` | Mailbox (`/email`), **24** `email_*` tables live at pin (audit corrected from 26), Gmail push sync (watch→GCP Pub/Sub→webhook), two-phase Gmail-API send w/ undo (no SMTP), Redis backfill counters, delegation + shared-inbox promotion. Full audit: `audits/company-mailbox-audit.md`. GCP Pub/Sub stays a hard dependency. | ~~Drop (Instantly instead)~~ — framing corrected 2026-08-19 | Workers + Queues (sync), DO/D1 (threads), R2 (attachments) | **Keep, IN the pilot — full faithful recreation incl. send (2026-08-19)** |
| ✔ | **Agent connectivity layer** (new — replaces the "Instantly read-only" framing) | new concept; seams: `gatekeeper-mcp`, kernel session APIs; old analogues `crates/mcp_client`, `crates/import` (gather/staging ledger — strongest analogue), `crates/foreign_entity`, `features/integrations`. Full audit: `audits/connectivity-layer-audit.md` (9-gap delta; no Instantly connector exists in either codebase; old catalog = hardcoded FE constant) | Composio-style MCP + API linking so agents pull external data into the workspace (Instantly = first source) | — | Gatekeeper connectors + MCP catalog | **Build as the P1 outreach centerpiece (2026-08-19, per David's clarification)** |
| ◐ | notification_service | `services/notification_service` (2,032 LOC shell) over **`crates/notification` (61 files, 9,703 non-test LOC)**, `crates/model_notifications`, `crates/notification_db_client`, `crates/graphql_notification`; type catalog `crates/model_notifications/src/metadata.rs` (22 metadata structs, **19 `const TYPE_NAME`**); egress `crates/notification/src/domain/service/egress.rs:64,101-140`; workers `src/inbound/{ingress_worker,worker,push_notification_event_worker,notification_events_listener}.rs`; SNS `src/outbound/sns_endpoint.rs`; VoIP `src/domain/service/voip.rs`; ~22 endpoints (EB §6); 11 tables (SH §4). Full audit: `audits/standalone-services-audit.md` §A | **The largest unruled capability in the ledger.** **19 notification types**: `ai_response`, `call_started`, `channel_mention`, `channel_message_reply`, `channel_message_send`, `commented_on_document`, `document_mention`, **7× `github_pr_*`/`github_review_requested`**, `inbox_reauth_required`, `mentioned_in_document_comment`, `new_email`, `reminder`, `replied_to_document_comment_thread`, `task_assigned`. Each type implements `format_title`, so **user-visible copy lives in Rust**, not a template store. **Three egress channels behind one state machine**: WebSocket (via connection_gateway), **mobile push through AWS SNS platform endpoints** (ios/android/**iosvoip** for CallKit — coupled to the ruled-keep calls row), and **batched email digests** with unsubscribe codes and per-channel dedupe. Delivery is gated by a **last-online checker** (push only when the user is not live on the socket) and a rate limiter; successes record an SNS message id in `notification_message_receipt`. Preferences are per-type opt-out, per-item mute, and a global mute. There is also an **AI-tool surface** — agents can raise notifications. Endpoints cover list/bulk-get/seen/done/undone/delete, preferences, and unsubscribe. | Drop | **The provisional "Drop" predates the rulings and is now load-bearing.** Every producer of these 19 types is ruled **keep**: channels (full), mailbox (in pilot), CRM, calls, calendar, reminders, documents/tasks, and GitHub — GitHub alone accounts for 7 of the 19. Dropping the domain does not remove the work, it **relocates** it, because "mention someone and they find out" is a channels requirement. CF-native shape if kept: fan-out is **D3/3a** (intent record + alarm, idempotent per recipient); in-app delivery is **C1** kernel session events; email digests are a **D4/4a** per-user alarm plus the mail path; **mobile push is the one piece with no kernel analogue at all** — APNS/FCM would be a new Worker-side integration (SNS does not come along), and it only matters if native apps survive (which also interacts with deferral **C3** `/.well-known` / native-app-links). Effort: **large if kept, and not zero if dropped.** | — |
| ✔ | contacts_service | `services/contacts_service` | **Research correction (2026-08-19 audit): NOT CRM** — a user↔user connections graph (`contacts_connections(user1,user2)`, SQS-fed from channel membership + auth events; `crates/contacts/src/inbound/http.rs:217-245`). The CRM has no dependency on it. See `audits/crm-audit.md` §A3. | Keep (CRM) | Standalone user-connections graph, CF-native | **Re-ruled (2026-08-19, batch E): keep as its own capability, in the pilot** — rebuilt CF-native as a standalone user↔user connections graph (mention/share-suggestion feeder), independent of CRM; supersedes the wrong-premise "keep with CRM" verdict |
| ◐ | static_file_service | `services/static_file_service` (1,966 LOC / 31 files); routers `src/api/mod.rs:75-92` (the **same** file router mounted at both `/api` and `/internal`), `src/api/file/mod.rs:13-23`; **DynamoDB** config `src/config.rs:22`; S3-event upload finalize `src/api/event/s3_create.rs:7-19`; models `src/model/api.rs:8-25`; spec `packages/sdk/specs/static-files.json` | Generic blob storage for everything that is not a `Document`. 6 endpoints per mount: `GET /api/file/metadata/{id}`, `GET /api/file/{id}/presigned-url`, `PUT /api/file` (returns a presigned **upload** URL + id), `DELETE /api/file/{id}`, `POST /api/file/bulk-delete`, `/api/health`; plus a CDN path `GET /file/{file_id}` served by CloudFront/S3, not by axum. **Finding: file metadata lives in DynamoDB, not Postgres** (`config.rs:22`; every handler goes through `service::dynamodb::client::DynamodbClient`) — `schema-harvest.md` §6 records DynamoDB only for `BulkUploadRequest`, so **this is a second, unharvested non-Postgres store** holding name, content type, `extension_data JSONB` and upload state for every static file in the product. Lifecycle is the same **pending→ready** machine as documents: presigned PUT → direct browser upload → **S3 event → `mark_uploaded(file_id)`**; the handler explicitly skips CloudFront image-optimizer derivative keys (`file/{uuid}/format=webp,width=300`), which is where the `image_optimizer` Lambda writes. Couplings: email attachments (`email_attachments_sfs`, `email_sfs_mappings`, and the `email_sfs_delete_handler` Lambda that GCs orphans), chat attachments (`ChatAttachment.entity_type='static_file'`), and `EntityType::StaticFile` as one of the 16 canonical variants. | Replaced | **R2 + Workers**, with three specifics the row must carry. (1) The `/api/file/*` mount is **collision #1** in `route-reconciliation.md` §7 and dies with **R3**; file GETs are one of the explicitly-allowed HTTP surfaces in the ruled hybrid, so this becomes signed R2 URLs or a `/files/*` worker route (`route-reconciliation.md` §6). (2) The **unharvested Dynamo metadata table** must be read before the entity shape is designed — it is not in `schema-harvest.md`. (3) The S3-event → `mark_uploaded` hop becomes an **R2 event notification → Queue** consumer, idempotent per **D3/3a**, and it is the *same build* as the documents ingestion family (`audits/lambda-batch-families-audit.md` §B) — one content-state machine, two callers. Effort: small-medium. | — |
| ◐ | unfurl_service | `services/unfurl_service` (1,446 LOC / 15 files); routers `src/api/mod.rs:56-57`, `crates/unfurl/src/inbound/axum_router.rs:50`, `src/api/proxy/mod.rs:102`; tag extraction `src/unfurl/mod.rs`; **SSRF guard** `src/http_safety/mod.rs:91,109`; timeouts `src/main.rs:24-25`; spec `packages/sdk/specs/unfurl.json` | Link preview cards. 4 endpoints: `GET /unfurl?url=`, `POST /unfurl/bulk`, `GET /proxy`, `/health`. Fetches a URL and parses OpenGraph/Twitter meta tags into a card (keys of the form `property:og:title` / `og:description` / `og:image`, with `og:site_name` as a title fallback). **8s request / 3s connect timeouts.** Hardening is the substantial part: `validate_url` → `assert_not_internal` (**resolves the host and rejects private/loopback/unspecified IPs**), redirect-count limits, content-type checks, `Content-Length` checks **and** a streaming size cap so a lying `Content-Length` cannot exhaust memory. Also the enrichment **fallback resolver for the CRM domain directory** when Apollo has no answer (`audits/crm-audit.md` §D5) — so the CRM row depends on it. Note the `/proxy` path is a **literal collision** with `image_proxy_service`'s `/proxy` (`route-reconciliation.md` §7-11). | — | A Worker `fetch` under **R3** (RPC method for the card; the raw `/proxy` byte path is HTTP-required). Two things do not come free. (1) **The SSRF guard has no Workers equivalent** — Workers cannot resolve a hostname and then decide whether to fetch it, so the check-then-use pattern is unportable; this is one shared ruling across unfurl, image-proxy, **outbound webhooks** and connector endpoints (`audits/standalone-services-audit.md` §C1). (2) **No cache layer was found in this service** (absence not proven — see the audit's coverage), so a CF rebuild should decide caching deliberately rather than assume parity; Cache API or KV is the obvious slot and would also serve the CRM directory's negative-cache behavior. Rule **with `image_proxy_service`** — same substrate question, same colliding path. Effort: small. | — |
| ◐ | image_proxy_service | `services/image_proxy_service` (1,160 LOC / 13 files); router `src/api/mod.rs:48`, handler `src/api/proxy/mod.rs:432`; limits/behavior `src/api/proxy/mod.rs:26,33,43,195,260,380-420`; **no OpenAPI spec** | **It does not transform images.** Despite the name, `GET /proxy` is a streaming pass-through fetch: 10 MB cap (enforced on `Content-Length` **and** during streaming), 15s request / 5s connect timeouts, **manual redirect following** (max 5, re-validating each hop), a **spoofed desktop-browser User-Agent** so hotlink-blocking origins serve the bytes, and `Cache-Control: public, max-age=31536000, immutable` on the response. Its purpose is CORS/hotlink/referrer laundering for remote images — most visibly **inline email images** (`upsert_message.rs` maps inline images to SFS; remote ones go through here) — not resizing. Resizing in the old system is a *different* component: the `image_optimizer` Lambda writing CloudFront derivative keys (`audits/lambda-batch-families-audit.md` §F). 2 endpoints total; `/proxy` collides literally with `unfurl_service`'s `/proxy` (`route-reconciliation.md` §7-11). | Replaced | **Cloudflare Images / Image Resizing** is a direct replacement and is strictly more capable — it also absorbs the separate `image_optimizer` Lambda, so these two ledger items collapse into one decision (`audits/lambda-batch-families-audit.md` §G). The same **SSRF-by-DNS blocker** as unfurl and webhooks applies to any user-supplied image URL (`audits/standalone-services-audit.md` §C1). Path collision dissolves under **R3** (distinct prefixes or an RPC-fronted fetch). Worth stating explicitly for the rebuild: nothing here needs to be *ported* — the row is a decision about whether remote-image laundering is still needed once the mailbox is rebuilt. Effort: **small**, and possibly zero. | — |
| ◐ | search_processing_service | `services/search_processing_service` (10,165 LOC); consumers `services/search_processing_service/src/inbound/kafka_consumer/{call,channel,chat,document,email,project,property}.rs`; internal API `src/api/internal/{backfill,delete_document,extract_sync}.rs`; jobs `src/domain/jobs.rs` | **Not a Lambda — a long-running Kafka consumer**, and the real indexing path (`search_upload_handler` is a small EventBridge side-job; see `audits/lambda-batch-families-audit.md` §C). One consumer module per entity type — **7**: call, channel, chat, document, email, project, property — matching `search_service`'s 7 searchable types. Also exposes an internal HTTP surface: `/backfill` (re-index), `/delete_document`, `/extract_sync` (pull text for sync-service-backed docs — the `document.sync_content_updated` path from `audits/documents-audit.md` §C). This is the consumer half of the **14-topic `macro.*` Kafka bus** (`macro.documents`, `.projects`, `.properties`, `.chats`, `.channels`, `.email`, `.calls`, `.teams`, `.bots`, `.mentions`, `.soup`, `.webhooks`, `.activity_events`, `.com`). | Defer | Kafka → **Cloudflare Queues**, one consumer per entity type, idempotent per **D3/3a** with poison-pill mark-and-skip (re-indexing the same entity twice must be a no-op — it already is, by index-by-id). Backfill is a bulk job → DO with alarm under **D4/4a**, not a dispatcher. The `macro.*` topic list is the **event-bus contract for the whole rebuild**, not just search — it is the single most reusable artifact from this row. **Rule with `search_service`** — one system, two halves. Effort: medium; the substrate decision is shared with `search_service`. | — |
| ◐ | scheduled_action | `services/scheduled_action` (2,464 LOC / 21 files — **path verified**); router `src/inbound/axum_router.rs:50-62`; models `src/domain/models.rs:18-38,53,58`; **polling dispatcher** `src/outbound/pg_polling_dispatcher.rs:20,24,22-30`; executor `src/outbound/inprocess_executor/agent_task.rs`; live push `src/outbound/conn_gateway_live_updates.rs`; tables `scheduled_action`, `action_execution_record` (SH §1.8, `20260416135258_scheduled_agent.up.sql`); spec `packages/sdk/specs/scheduled-action.json` | **"Automation" means "a scheduled chat".** 7 endpoints: `GET`/`POST /scheduled-actions`, `PUT`/`DELETE /scheduled-actions/{id}`, `POST /scheduled-actions/{id}/execute` (run now), `GET /scheduled-actions/{id}/history`, `/health`. **`ActionKind` has exactly one variant — `Agent`** (`models.rs:53`); `AgentTask` is `{model, prompt, user_prompt}`. Execution **creates a real `Chat` row** and runs the full `all_tools` AgentLoop with the user's `memory` injected, then notifies on completion — which is why the frontend surfaces automations inside the **`agents`** view (EF §2, `automations` tab) and gives them the `automation` block. Schedule = 6/7-field cron + IANA timezone → `next_run_at`. **Dispatch is the weakest lease tier in the platform**: batches of 10, a 30s floor per loop, claim-by-atomic-conditional-UPDATE with a stale-or-unclaimed filter, and the file's own comment explains it exists so peer instances can share a backlog (`pg_polling_dispatcher.rs:22-30`; catalogued in `pattern-review.md` §4 as "bare timestamp"). Progress/live updates go out via connection_gateway. | Replaced | **D4/4a** rules this shape out explicitly: per-action DO alarms replace both the polling dispatcher and the claim column — this is the canonical "no ported dispatchers" case alongside the two 1-minute Lambda polls (`audits/lambda-batch-families-audit.md` §G5). Because the only action kind is "run an agent", the CF-native row is plausibly **not a service at all** but *(saved prompt + schedule) → kernel agent session*, which also collapses the functional overlap with the kernel's existing **`gatekeeper-scheduler`** already flagged in `route-reconciliation.md` §7-19. Execution history is a small per-action collection; live updates become **C1** session events; CRUD is RPC under **R3**. The one thing worth harvesting verbatim is the **cron + IANA-timezone** semantics (DST correctness is easy to get wrong). Effort: small. | — |
| ◐ | convert_service | `services/convert_service` (1,365 LOC / 15 files — **path verified**); routes `src/api/mod.rs:46-50`, `src/api/convert.rs:33-39`, `src/api/backfill/mod.rs:7`; **LibreOffice binding** `services/convert_service/Cargo.toml:45`; container `docker/Dockerfile.convert_service:38-40`; request shape `crates/model/src/convert/mod.rs:5-28`; **no OpenAPI spec** | **The second hard substrate gap in the merge.** 3 endpoints, all internal/service-key: `POST /internal/convert`, `POST /internal/backfill/docx`, `/health`; also drivable off a queue (`ConvertQueueMessage` adds a `job_id`). The job is **bucket→bucket**: `{from_bucket, to_bucket, from_key, to_key}`. **It embeds LibreOffice** — `rs-libreoffice-bindings` (a `macro-inc` git dep pinned to `056a40d`) over **Collabora Online `core-co-25.04` assets**, and the image accepts the **Microsoft core-fonts EULA** so DOCX renders with correct metrics. This is the producer of `DocumentContentLocation::ConvertedPdf` — i.e. the legacy `Docx → ConvertedPdf` mapping in `audits/documents-audit.md` §B2 — so it is the DOCX half of the documents content model, not a side utility. Full context: `audits/standalone-services-audit.md` §E. | — | **No Workers-native successor exists.** This is the same class of problem as `ffmpeg` in the call-recording-preview handler (`audits/lambda-batch-families-audit.md` §F/§G4) and should be ruled **with it and with the `documents` content-location model**, not alone. The realistic options are a **Cloudflare Container**, an external conversion API, or dropping DOCX rendering (which would also drop the `DocumentBom`/`BomPart` docx-parts machinery on the documents row and the `docx_unzip_handler` ingestion Lambda). Everything *around* the converter ports cleanly — R2 object in, R2 object out, job triggered by a Queue message, idempotent on `to_key` per **D3/3a**. Effort: **small if dropped, container-shaped if kept** — the decision is a substrate/product one, not an engineering one. | — |
| ◐ | mcp_service + mcp_auth_proxy | `services/mcp_service`, `services/mcp_auth_proxy` (verified) | Macro-as-MCP-server (first-party toolset to external agents) + an OAuth 2.1 broker that exists **because FusionAuth lacks DCR** (`mcp_auth_proxy/README.md:1-6`) — substrate-motivated; FusionAuth is dead. See `audits/connectivity-layer-audit.md` §A2. | Replaced | `gatekeeper-mcp`; the auth-proxy role dissolves with the auth rebuild | — |
| ✔ | sync-service (deployable) | `services/sync-service` (Rust/WASM, DO+D1, 18 routes, WS) | Collaborative-editing sync — already Cloudflare | — | Lift as-is (WASM exception to the no-Rust rule) | **Lift all four CF services (2026-08-19).** Integration requirement: their routes must be reconciled into the unified CF-OS route model — the old build conflict was these services syncing against AWS-side routes; now route/service alignment is a first-class design task of the rebuild. |
| ✔ | lexical-service / ai-editing-worker | CF Workers in repo | Editor AI — already Cloudflare | — | Lift as-is | **Lift (2026-08-19)** — same route-reconciliation requirement |
| ✔ | coding-agent-worker | ~~CF Worker in repo (TS)~~ **Research corrections (2026-08-19): EMPTY at the pin**, and a full history/all-refs search of both repos found **no source ever committed anywhere**; the lockfile (`name: daytona-bun-hello` — Bun + Hono + Daytona SDK + Ink CLI, zero Cloudflare deps) was a swept-in local prototype artifact and **was never a Cloudflare Worker**. The commit that added it delivered `crates/agent_runtime_protocol` (WS/ACP server half, Rust). See `route-reconciliation.md` §4. | ~~Agent runtime worker — already Cloudflare~~ (mislabel from a hedged platform-canvas doc line) | — | ~~Lift~~ nothing to lift | **Re-ruled (2026-08-19, route ruling A3): dropped from the lift set** — the lift set is three services (sync, lexical, ai-editing); supersedes the "Lift" verdict, which had no object. Future intent tracked in the row below. |
| ☐ | Coding-agent capability (future) | Reference only: `crates/agent_runtime_protocol` (Rust WS/ACP server half) + the dropped worker's bun.lock revealing the intended design (Bun + Hono + Daytona sandboxes + Zed agent-client-protocol + Ink CLI) | Sandboxed coding-agent runtime — never built in Neuwave | — | If ever wanted: new scope designed on the kernel's own agent runtime, not a lift | **Marked future-only (2026-08-19, ruling A3)** — not pilot scope; no old system exists to recreate |
| ◐ | analytics-proxy | `services/analytics-proxy` (**path verified**; TS + Hono + `wrangler.jsonc`, **already a Cloudflare Worker**, zero AWS dependency); `src/index.ts:8-19,21,26,35-41,78,106-108` | A first-party telemetry front door, and **it is already running on Cloudflare**. Two proxy prefixes. **`/i/ph` → `https://us.i.posthog.com`**: a same-origin PostHog proxy that additionally **renames the session-replay recorder script** to `runtime.js` on the browser-facing path, with the source comment stating the reason — *"privacy filter lists block the upstream filename"* — i.e. deliberate ad-blocker evasion for session recording (a product/privacy decision, not plumbing). **`/i/otlp/v1/traces` and `/i/otlp/v1/logs` → per-signal Datadog OTLP intakes**, split because Datadog has no single OTLP origin, with the server-side `DD_API_KEY` injected by the Worker so the browser never holds it (the other providers rely on a client-side token already on the request). Local dev points the same paths at a plaintext otel-collector. `route-reconciliation.md` §7-20 already resolves the routing question: `/i/*` is collision-free and can stay as-is. | Replaced | Not a port — the code already runs on the target platform, so the row is **a stance, not an engineering task**: does Outreach-OS keep PostHog + Datadog, or adopt the kernel's own feature-flag/analytics position (`../cf-os-capability-map.md`)? Whatever is chosen, the Worker's two real contributions are worth keeping deliberately: **server-side secret injection** (an API key that must never reach the browser) and **first-party-origin telemetry** (the whole reason the proxy exists). The recorder-rename behavior should be an **explicit** decision rather than an inherited one. Note the same-origin argument is weaker under the ruled single-origin router than it was across `*.macro.com` hostnames. **Cross-ref (A3):** this is one of **six** Cloudflare Workers at the pin and one of **three outside the ruled lift set** — `audits/standalone-services-audit.md` §G. Effort: **near-zero**. | — |
| ✔ | transcription (LiveKit sidecar) | verify path | Call transcription | ~~Drop~~ | TBD at design time | **Keep with calls (2026-08-19)** |
| ◐ | Lambda/batch families (**exactly 20 handlers + 1 ECS worker, 7 families**) | Every crate in `services/*` linking `lambda_runtime`/`lambda_http` (enumeration method: `audits/lambda-batch-families-audit.md` §H); schedules in `infra/stacks/**` `scheduleExpression` | **The inventory hole is closed — the count is exact, not "~20".** Ingestion (5): `document_upload_finalizer_handler`, `docx_unzip_handler`, `upload_extractor_lambda_trigger`+`_handler`, `document_text_extractor`. Search (1): `search_upload_handler`. Email (4): `email_refresh_handler` `rate(1 hour)` — re-arms expiring Gmail `users.watch`, i.e. **push sync silently dies without it**; `email_scheduled_handler` `rate(1 minute)`; `email_sfs_delete_handler` `cron(0 8 * * ? *)`; `email_suppression_handler` (**SNS from SES** bounce/complaint → suppression list). Retention (6 + ECS): `deleted_item_poller` `rate(4 hours)` (>30-day purge), `delete_chat_handler` (SQS), `organization_retention_trigger` `rate(1 day)`+`organization_retention_handler` (SQS), `user_link_cleanup_handler` `rate(8 hours)`, `worker_trigger` `rate(1 hour)` → **`sha_cleanup_worker`** (ECS task, **Redis** `bucket:sha-delete` work set). Media (2): `call_recording_preview_handler` (S3, **ffmpeg**), `image_optimizer`. AI (1): `ai_projections_refresh_handler` on **3 cadence tiers** (6h/1d/3d). Security (1): `dataloss_prevention_handler` (S3, daily) — **scans and deletes user content**. Full audit incl. per-handler triggers: `audits/lambda-batch-families-audit.md`. | Never mapped | Per family, not per handler. **Most of these do not port — they dissolve:** under **D4/4a** the four trigger→handler pairs collapse into one DO with an alarm (the split is a 15-minute-Lambda artifact), and the two 1-minute polls (`email_scheduled_handler`, plus the reminder dispatcher at `infra/stacks/cloud-storage-service/reminder-dispatch-queue.ts:60`) are exactly the "no ported dispatchers" case — per-item DO alarms replace them. Ingestion → R2 event notifications + Queues, idempotent per **D3/3a**, keyed on document id + sha; it is the same build as the `documents` row's `pending`→`ready` content-state machine. Only the Gmail `watch` re-arm is inherently periodic and must stay so. **Four things need David, not design:** (a) this row should become ~7 rows attached to their domains — audit §G1; (b) **DLP has no ledger row** and deleting user content on policy is a product decision; (c) **Redis** appears as infrastructure with no CF successor named (every use found is a queue or counter → DO state under D3/D4, but say so as a ruling); (d) **ffmpeg has no Workers-native successor** — Container, Media Transformations, or drop call previews. Effort: spread across families; individually small-to-medium. | — |

## 4. Frontend surfaces — routes & splits

From EF §1–2 (27 routes, 28 registered splits; 24 dev-only splits excluded —
collective row at bottom). The split-layout model itself (`/*splits` encoding
alternating `{type}/{id}` pairs) is its own row: it is the old shell's core
navigation idea and the shell plan flagged it as a conflict with CF-OS's
simpler routing.

| St | Capability | Old source | What it is | Verdict |
|---|---|---|---|---|
| ✔ | Split-layout engine | `apps/web/src/components/app/split-layout/`, `Root.tsx:223` | Multi-pane `{type}/{id}` navigation model — the shell's spine | **Recreate faithfully as the new app's shell, on CF-OS's frontend (2026-08-19).** Supersedes the shell plan's Gatekeeper-App-vs-custom-shell question. |
| ✔ | Soup list engine (SoupView) | `apps/web/src/features/next-soup/` | The unified list engine: filters, sorting, presets, sidebar; backs 10 of the splits | **Recreate faithfully; data via native RPC not GraphQL (2026-08-19)** |
| ◐ | `home` | `features/home` (EF :53) | Chat-first landing view with composer | — |
| ◐ | `inbox` | `features/next-soup` (EF :55) | Unified inbox list | — |
| ◐ | `tasks` + `task-compose` | `features/next-soup` (EF :64), `features/block-md` (:75) | Task list + composer — the Work-tab heart (SUP-493/495) | — |
| ◐ | `agents` | `features/next-soup` (EF :61) | Agents/automations list | — |
| ✔ | `mail` + `email-compose` | `features/next-soup` (EF :62), `features/block-email` (:74) | Email list + composer | **Keep — mailbox ruled keep-in-pilot (2026-08-19)** |
| ◐ | `documents` / `files` / `folders` | EF :63/:68 | Files and folder lists | — |
| ✔ | `channels` + `channel-compose` + `non-member-channel` | EF :65/:73/:72 | Channel list, composer, join prompt | **Keep — channels ruled keep-full (2026-08-19)** |
| ✔ | `companies` (CRM view) | `features/next-soup` + `features/companies` (EF :67) | CRM saved views | **Keep — CRM ruled keep-in-pilot (2026-08-19)** |
| ◐ | `search` | EF :69 | Search results list | — |
| ✔ | `calendar`, `reminders`, `calls`, `activity` | EF :56–58, :66 | Flag-gated secondary views | **Keep all four (2026-08-19)** |
| ◐ | `settings` (12 active tabs, 3 groups) | `features/settings`, `settingsTabsConfig.tsx:48` (EF §6) | Account/Billing/Appearance/Team/Tags/CRM/Connections/MCP/Bots/Admin… | — |
| ◐ | Command menu / launcher | `features/command/CommandMenu.tsx`, `Launcher.tsx` | Cmd-K command surface | — |
| ✔ | Auth screens (login/signup/invites) | `features/auth`, `team-invitations`, `channel-invitations` | Auth UI | **Keep — real-auth ruling (2026-08-19)** |
| ◐ | `getting-started`, onboarding (`setup`), paywall | `features/getting-started`, `setup`, `onboarding`, `paywall` | First-run and billing chrome | **Parked to end (2026-08-19)** — something different planned; document primitives from source as reference |
| ◐ | `import-linear` + integrations | `features/integrations/` | Linear import, MCP setup | — |
| ◐ | Entity layer + property system + sharing + favorites | `features/entity`, `features/property`, `features/sharing`, `features/favorites` | Cross-cutting UI infrastructure behind blocks/lists | — |
| ◐ | Dev-only splits (24) | EF §2 dev lists | Debug/playground pages | — (default: not recreated) |

## 5. Block types (16 concrete + aliases)

From EF §3; definitions at `apps/web/src/features/block-*/definition.ts`,
registry `apps/web/src/lib/core/block.ts:50`. Aliases: `csv`→code;
`task`/`snippet`/`skill`→md (so Task is an md-block flavor — matters for the
task model design). `write` is virtual; `unknown` is the fallback.

| St | Block | Old source (definition) | Verdict |
|---|---|---|---|
| ◐ | `md` (+ task/snippet/skill aliases) | `features/block-md/definition.ts:26` | — |
| ◐ | `chat` (AI chat) | `features/block-chat/definition.ts:18` | — |
| ✔ | `channel` | `features/block-channel/definition.ts:6` | **Keep — channels ruled keep-full (2026-08-19)** |
| ✔ | `email` | `features/block-email/definition.ts:6` | **Keep — mailbox ruled keep-in-pilot (2026-08-19)** |
| ✔ | `company` / `contact` (CRM) | `features/block-company/definition.ts:6`, `block-contact/definition.ts:6` | **Keep — CRM ruled keep-in-pilot (2026-08-19)** |
| ◐ | `project` (folder) | `features/block-project/definition.ts:13` | — |
| ◐ | `code` (+ csv) | `features/block-code/definition.ts:12` | — |
| ◐ | `pdf` / `image` / `video` | `block-pdf/definition.ts:15`, `block-image/definition.ts:14`, `block-video/definition.ts:52` | — |
| ◐ | `canvas` | `features/block-canvas/definition.ts:15` | — |
| ◐ | `automation` | `features/block-automation/definition.ts:6` | — |
| ◐ | `pr` (GitHub PR) | `features/block-pr/definition.ts:5` | — |
| ✔ | `call` | `features/block-call/definition.ts:15` | **Keep — calls ruled in (2026-08-19)** |
| ◐ | `unknown` (fallback) | `features/block-unknown/definition.ts:11` | — |

## 6. Cross-cutting invariants (from the original plan's "Do not break" list)

Each needs an explicit ruling: preserved in the recreation, or consciously
dropped. See `original-linear-plan-review.md`.

**Ruled 2026-08-19 (Q20): all five preserved as binding design invariants.**
The `ai_toolset` freeze applies to its semantics as recreated in Gatekeeper
session APIs (Q18).

| St | Invariant | Verdict |
|---|---|---|
| ✔ | Soup/Block/Split/Entity/Team/Channel nouns | Preserved (2026-08-19) |
| ✔ | Frozen `ai_toolset` | Preserved — semantics carried into Gatekeeper session APIs (2026-08-19) |
| ✔ | Share-permission semantics (cross-ref SEC-1/2/3 holes, SUP-474) | Preserved — with the SEC holes fixed, not recreated (2026-08-19) |
| ✔ | Project = Folder | Preserved (2026-08-19) |
| ✔ | One Task database | Preserved (2026-08-19) |

## 7. Data model

From SH. **Key correction to the concept docs:** the "four Postgres databases"
are one physical database — 198 live tables, one migration stream at
`crates/macro_db_client/migrations/` (267 files), logical grouping only by
table prefix + client crate, with real cross-domain FKs. Design patterns that
shaped the old system and need a deliberate adopt/reject ruling: polymorphic
`entity_type`/`entity_id` pairs as universal glue (10+ tables, no FK),
transactional outbox tables, lease-based job claiming. Also: DynamoDB
(bulk-upload requests), Redis (cache + email backfill state), S3 blobs
(sha-addressed), client-side SQLite cache — and **two schemas already on D1**
(`services/sync-service/database/user-peer-mapping`, `services/ai-editing-worker`
edit_traces). One-file schema reference incl. all enums:
`basic_schema_for_testing.sql` (test snapshot carried by five workers).
No live data migrates (ruling 8) — these rows decide which *shapes* the new
D1/DO typed-storage design adopts.

| St | Table group | Old source (prefix / SH section) | Size | Verdict |
|---|---|---|---|---|
| ◐ | Identity, teams, membership | MacroDB group | part of 154 | — |
| ◐ | Documents + versions + annotations | MacroDB group | " | — |
| ◐ | Projects (folders) | MacroDB group | " | — |
| ◐ | Sharing / ACL | MacroDB group (cross-ref SEC-1/2/3) | " | — |
| ◐ | AI chat, insights, projections | MacroDB group | " | — |
| ◐ | Properties (EAV) | MacroDB group | " | — |
| ✔ | CRM (companies/contacts) | MacroDB group | " | **Adopt shapes — CRM ruled keep-in-pilot (2026-08-19)** |
| ✔ | Calls/voice, calendar | MacroDB group | " | **Adopt shapes — both ruled keep (2026-08-19)** |
| ◐ | GitHub, bots, webhooks, import, reminders, activity | MacroDB group | " | — |
| ✔ | Email (`email_*`) | 26 tables | — | **Adopt shapes — mailbox ruled keep-in-pilot (2026-08-19)** |
| ✔ | Comms (`comms_*`) | 7 tables | — | **Adopt shapes — channels ruled keep-full (2026-08-19)** |
| ◐ | Notifications | 11 tables | — | — |
| ✔ | Polymorphic entity glue pattern | 10+ tables; canonical type `model-entity` (56 dependent crates) | pattern — **reviewed 2026-08-19**: domain-motivated ontology, substrate-shaped encoding; options 1a/1b/1c in `pattern-review.md` §1 | **Ruled 1a (2026-08-19): adopt the ontology, replace the encoding** — `Entity=(type,id)` universal; entities in DOs/typed-storage; entity registry (existence + tombstones); designed materialized-index layer for cross-entity lists (required); per-type access policy stays code. Rider: settle TEXT/UUID id format before building glue. |
| ✔ | EAV property system pattern | properties tables; 18 system keys incl. task Status/Assignees | pattern — **reviewed 2026-08-19**: custom props domain-motivated, system-fields-as-EAV substrate-motivated; the query half is the kernel's one genuine gap; options 2a/2b/2c in `pattern-review.md` §2 | **Ruled 2a (2026-08-19): adopt semantics, replace storage** — definitions/options/tagged-values/system-key semantics kept exactly; values live on the entity record (single-writer DO); materialized-index layer for list filtering is a first-class design task. Riders: settle entity-type canonicalization first (TASK/DOCUMENT dual-namespace bug); property-write side effects become cross-DO effects needing pattern-3 treatment. |
| ✔ | Outbox + lease-claim job patterns | migrations (`20260725014930` et al.) | patterns — **reviewed 2026-08-19**: both ~purely substrate-motivated; kernel already has DO-native idioms (intent+alarm outbox; DO-as-lease); keep external-mutation fencing; options 3a/3b + 4a/4b in `pattern-review.md` §3–4 | **Ruled 3a + 4a (2026-08-19): adopt the disciplines, not the tables** — outbox → intent record + alarm inside the DO, idempotent consumers, poison-pill mark-and-skip; leases → DO single-writer + alarms replace claim/renew/release and polling dispatchers; external-mutation fencing + idempotency keys kept as DO-local state; multi-job DOs use the recompute-shared-alarm discipline. |

**Pattern-review workstream (ordered by David 2026-08-19, Q21):** David leans
*adopt*, but each pattern must first be reviewed by an agent against the
source to determine **why it exists** — domain-motivated (the product needs
it) vs. substrate-motivated (a Postgres/AWS/old-agent workaround). Each
pattern then gets a per-pattern adopt/replace proposal mapped explicitly onto
Cloudflare behavior (DO single-writer, alarms, Queues), so we never import a
system that works one way while the new substrate expects it to behave
another. David rules per pattern after that review.
| ◐ | Already-D1 schemas (sync peer-map, edit_traces) | `services/sync-service`, `services/ai-editing-worker` | 2 | — |
